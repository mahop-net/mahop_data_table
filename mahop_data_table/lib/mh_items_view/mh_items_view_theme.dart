import 'package:flutter/material.dart';

/// Here you can override and finetune all colors, sizes and so on
/// - The Items view is using Materia3 DataTableTheme as default
/// - Here you can override many things like hovercolor, selection color and many more
class MhItemsViewTheme {
  Color? rowBackgroundColor;
  Color? textColor;

  Color? headerBackgroundColor;
  Color? headerTextColor;

  Color? headerSeperatorColor;
  Color? rowSeperatorColor;
  Color? colSeperatorColor;

  Color? markedBackgroundColor;
  Color? markedTextColor;
  Color? selectionBackgroundColor;
  Color? selectionTextColor;

  Color? editBackgroundColor;
  Color? editTextColor;

  Color? hoverBackgroundColor;
  Color? hoverTextColor;
}
